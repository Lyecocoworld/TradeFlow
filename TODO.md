# To-Do List

## `shop.yml` Configuration Review

- [ ] Analyze the structure of `shop.yml` and identify inconsistencies.
- [ ] Verify the data types and values for each configuration key.
- [ ] Check for duplicated or unused entries.
- [ ] Propose a new, cleaned-up structure for `shop.yml`.
- [ ] Document the new `shop.yml` format.

## General Improvements

- [ ] Review and optimize the Gradle build (`build.gradle`).
- [ ] Enforce code style using `checkstyle.xml` and potentially add more rules.
- [ ] Improve the existing documentation (`README.md`, etc.).
- [ ] Add unit and integration tests for critical components.
- [ ] Analyze and refactor the web application part of the project (`webapp` directory).
